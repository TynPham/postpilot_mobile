import React from "react";
import { Text, View } from "react-native";

const Dashboard = () => {
  return (
    <View>
      <Text className="text-primary">Dashboard</Text>
    </View>
  );
};

export default Dashboard;
